# /plan 删除右侧“档案老照片”栏最右侧圆形指示图标

## Summary（摘要）
- 目标：删除右侧侧栏「档案老照片」横向相册区域右侧的圆形提示图标（带 `ArrowRight` 的圆形按钮/提示）。
- 范围：仅影响右侧默认侧栏（`SidebarDefault`）的老照片相册提示，不改动图片本身、滚动点点指示（底部小圆点）与点击放大功能。
- 涉及文件：
  - [SidebarDefault.tsx](file:///Users/liuyi/Desktop/%E5%A4%A9%E7%A9%86%E5%9C%B0%E5%9B%BE%E7%BB%88%E7%BB%88%E6%9E%81/src/components/SidebarDefault.tsx)

## Current State Analysis（现状分析）
- 该“最右侧圆形指示图标”位于 `SidebarDefault.tsx` 的 “Gallery Navigation Hints” 区块（见 [SidebarDefault.tsx](file:///Users/liuyi/Desktop/%E5%A4%A9%E7%A9%86%E5%9C%B0%E5%9B%BE%E7%BB%88%E7%BB%88%E6%9E%81/src/components/SidebarDefault.tsx#L87-L94)）：
  - 仅在 `galleryImages.length > 1` 时渲染
  - 采用绝对定位 `absolute ... -right-2`，并在 hover 时显示
  - 内容为圆形容器 `w-8 h-8 ... rounded-full ... animate-pulse` 内含 `ArrowRight` 图标

## Proposed Changes（改动方案）
### A) SidebarDefault：删除圆形提示图标渲染块
- 文件：[SidebarDefault.tsx](file:///Users/liuyi/Desktop/%E5%A4%A9%E7%A9%86%E5%9C%B0%E5%9B%BE%E7%BB%88%E7%BB%88%E6%9E%81/src/components/SidebarDefault.tsx)
- 改动：
  - 直接移除以下 JSX 区块（不保留条件渲染）：
    - `{galleryImages.length > 1 && ( ... ArrowRight 圆形提示 ... )}`
- 影响评估：
  - 删除后不会影响相册横向滚动、hover 动效、底部小点点指示与图片点击放大
  - 不涉及 CSS 变更（该提示图标无独立 CSS 类依赖）

## Assumptions & Decisions（假设与决策）
- 这里的“最右侧圆形指示图标”指的是 `ArrowRight` 圆形 hover 提示，而不是底部的小圆点分页指示。
- 若你其实想删掉“底部小圆点指示”，则需要改动 [SidebarDefault.tsx](file:///Users/liuyi/Desktop/%E5%A4%A9%E7%A9%86%E5%9C%B0%E5%9B%BE%E7%BB%88%E7%BB%88%E6%9E%81/src/components/SidebarDefault.tsx#L96-L101) 的 “Visual indicator for horizontal scroll” 区块（本计划暂不包含）。

## Verification（验证步骤）
1) 本地类型检查：运行 `npm run lint`，确保无 TS/JSX 错误。
2) 页面回归：
  - 进入任意时期，右侧侧栏 “档案老照片” 相册仍可横向滚动
  - 最右侧不再出现圆形 `ArrowRight` 提示图标

